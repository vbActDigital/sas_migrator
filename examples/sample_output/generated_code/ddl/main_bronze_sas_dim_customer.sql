-- DDL for dim_customer in Snowflake Bronze layer
-- Generated by SAS-to-Snowflake Migration Toolkit

CREATE OR REPLACE TABLE SAS_MIGRATION.RAW.DIM_CUSTOMER (
  CUSTOMER_ID NUMBER(38,10),
  CUSTOMER_NAME VARCHAR(50),
  EMAIL VARCHAR(80),
  CPF VARCHAR(14),
  PHONE VARCHAR(20),
  BIRTH_DATE DATE,
  GENDER VARCHAR(1),
  INCOME NUMBER(38,10),
  SEGMENT VARCHAR(20),
  RISK_SCORE NUMBER(38,10),
  REGISTRATION_DATE DATE,
  STATUS VARCHAR(10),
  CITY VARCHAR(40),
  STATE VARCHAR(2),
  ZIP_CODE VARCHAR(10),
  LAST_UPDATE TIMESTAMP_NTZ
);

-- Metadata
COMMENT ON TABLE SAS_MIGRATION.RAW.DIM_CUSTOMER IS 'Customer dimension table migrated from SAS rawdata.customers_raw';
COMMENT ON COLUMN SAS_MIGRATION.RAW.DIM_CUSTOMER.CPF IS 'PII - Brazilian tax ID';
COMMENT ON COLUMN SAS_MIGRATION.RAW.DIM_CUSTOMER.EMAIL IS 'PII - Email address';
